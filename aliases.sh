alias m='docker run -v `pwd`:/host -it mbmbam-ya make'
alias n='docker run -v `pwd`:/host -it mbmbam-ya node'
alias b='docker run -v `pwd`:/host -it mbmbam-ya bash'
alias h='docker run -v `pwd`:/host -it mbmbam-ya hy'
